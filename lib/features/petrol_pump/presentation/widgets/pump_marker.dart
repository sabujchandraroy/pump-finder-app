import 'package:flutter/material.dart';

class PumpMarker extends StatelessWidget {
  const PumpMarker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.local_gas_station,
      size: 40,
    );
  }
}
